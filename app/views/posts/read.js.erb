var $readLink = $('.read[data-read="<%= @post.id.to_s %>"]');
$readLink.attr('data-status', '<%= @user_post.read.to_s %>');

if($readLink.closest('.indexFeed').length > 0){
  $readLink.closest('.article').hide()
}
